# LLM provider selection (added in a later slice; stub is used when unset)
# LLM_PROVIDER=groq
# GROQ_API_KEY=your-key-here
# GROQ_MODEL=llama-3.1-8b-instant

# Number of chunks to retrieve per query (default: 5)
# TOP_K=5
